{
  "name": "gov-osint-local",
  "version": "1.0.0",
  "private": true,
  "description": "Local authorized public-source investigation workspace",
  "scripts": { "start": "node server.js", "test": "node --check server.js && node --check app.js && node test.js" },
  "engines": { "node": ">=18" }
}
